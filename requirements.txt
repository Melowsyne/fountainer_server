# Fountain v2.2 Server (server/ + fountain_proto/ + run_server.py)
# HMAC authentication uses only the standard library (hmac/hashlib);
# so does the firmware HTTP server (http.server). The only runtime dependency
# is the WebSocket library.
websockets==12.0

# Admin web UI (login + RPC buttons), asyncio-native in the same loop.
aiohttp>=3.9

# Only for the tests:
pytest>=8.0
